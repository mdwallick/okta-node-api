{
  "name": "secret-generator",
  "version": "1.0.0",
  "description": "Generates base32 shared secrets for custom TOTP",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mike@wallick.net",
  "license": "MIT",
  "dependencies": {
    "speakeasy": "^2.0.0"
  }
}
